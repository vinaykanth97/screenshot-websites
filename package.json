{
  "dependencies": {
    "body-parser": "^1.20.1",
    "concurrently": "^7.5.0",
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "express": "^4.18.2",
    "nodemon": "^2.0.20",
    "puppeteer": "^19.2.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon app.js",
    "frontend": "(cd frontend && npm run start) & npm run start",
    "dev": "concurrently \"npm run server\" \"npm run frontend\""
  }
}
